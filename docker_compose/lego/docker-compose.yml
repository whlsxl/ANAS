version: "3.5"

services:
  lego:
    build: ./lego
    container_name: ${CONTAINER_PREFIX}lego
    image: lego
    restart: unless-stopped
    volumes:
      - "${LEGO_DATA_PATH}:/certs"
    env_file:
      - lego.env

networks:
  default:
    driver: bridge
    name: lego
